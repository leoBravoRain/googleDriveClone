sonar.projectKey=leoBravoRain_googleDriveClone
sonar.organization=leobravorain

# Project metadata
sonar.projectName=Google Drive Clone - Full Stack Project
sonar.projectVersion=1.0

# Source code locations for all components
sonar.sources=api-django-drf,backend,frontend

# Python version
sonar.python.version=3.11

# CI Analysis Configuration
sonar.ci.autoconfig.disabled=true
sonar.ci.autoconfig.includePlugins=false

# Test coverage (if you add coverage later)
# sonar.python.coverage.reportPaths=coverage.xml
# sonar.python.xunit.reportPath=test-results.xml

# Exclude patterns for all components
sonar.exclusions=**/migrations/**,**/__pycache__/**,**/venv/**,**/env/**,**/*.pyc,**/db.sqlite3,**/node_modules/**,**/dist/**,**/build/**,**/.git/**,**/data/**,**/scripts/**

# Python specific settings
# sonar.python.pylint.reportPath=pylint-report.txt

# Language detection
sonar.language=py,js,ts,html,css
